<ParamField body="objects" type="array" default="[]">
  An array of JSON objects, for example:

```example.json
[
    {"message": "Hi James!", "sender": "Bill"},
    {"message": "Hello!", "sender": "James"}
]
```

{" "}

<Note>
  The `id` field is reserved. If you need the `id` field to be stored, it needs
  to be stored as something else like `_id` or `$id`.
</Note>
An example object:

</ParamField>
