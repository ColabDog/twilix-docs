<ParamField body="splitOnField" type="string">
    Field to split on. This is suggested if you have 
    long documents to parse like a transcript. 
    All the splitting is done server-side and defaults to 
    `auto` as the split strategy - which uses the default 
    best splitter based on internal testing.
</ParamField>
