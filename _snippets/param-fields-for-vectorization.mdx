<ParamField body="fieldsForVectorization" type="array">
    A list of fields to vectorize. If None is supplied, it uses 
    all of them but this is NOT recommended - we suggest having a list of 
    fields to vectorize where possible.
</ParamField>
